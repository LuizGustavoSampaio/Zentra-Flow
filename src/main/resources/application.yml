spring:
  application:
    name: zentra-flow
  profiles:
    active: dev
  config:
    import: "optional:file:.env[.properties]"

  flyway:
    enabled: true
    locations: classpath:db/migration


logging:
  level:
    org.flywaydb: DEBUG
    org.springframework.jdbc: DEBUG